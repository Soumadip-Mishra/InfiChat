services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${PORT}:${PORT}"
    env_file:
      - .env
    command: ["nodemon", "--legacy-watch", "src/index.js"]
    volumes:
      - .:/app               
      - /app/node_modules 
    
